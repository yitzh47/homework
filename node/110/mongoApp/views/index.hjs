<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>{{ title }}</h1>


    <p>Welcome to {{ title }}</p>

    {{#blogs}}
      <h2>{{ title }}</h2>
      <p>{{date}}</p>
      <p>{{author}}</p>
      <p>{{ body }}</p>
    {{/blogs}}
  </body>
</html>
