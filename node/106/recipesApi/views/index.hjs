<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>{{ title }}</h1>
    <p>Welcome to {{ title }}</p>
    <a href='/allRecipes'>Returns json of all recipes.</a>
    <p>Endpoint is "/:name" (ex. "gonion", "oarlic(2)") for a specific recipe.</p>
    <p>Add recipe at endpoint "/addRecipe" (METHOD="POST"); make sure to input name, description, instructions and category Id.</p>
  </body>
</html>
